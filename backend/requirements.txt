# 运行时依赖（生产 / uvicorn 启动）
# 不要在此加 pytest / pytest-asyncio / pip-audit 等开发期工具；
# 那些在 requirements-dev.txt。

fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic>=2.11.0
pydantic-settings>=2.6.0
aiosqlite==0.20.0
SQLAlchemy>=2.0.37
python-dotenv==1.0.1
python-multipart==0.0.12
PyJWT==2.9.0
bcrypt==4.2.0

# EPUB 解析
EbookLib>=0.18
beautifulsoup4>=4.12
lxml>=5.0

# MP3 元数据（章节预告片按比特率截取）
mutagen>=1.47

# 对象存储（腾讯云 COS / S3 兼容协议）。选 boto3 而不是手写 SigV4：
# 签名、重试、分片上传都是成熟实现，避免自研密码学代码在生产踩坑。
boto3>=1.34