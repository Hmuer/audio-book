[pytest]
asyncio_mode = auto
# E-1：只保留一个导入根（仓库根目录 /workspace），测试统一用 backend.app.*。
# 之前是 "..:."，"." 让 /workspace/backend 也在 sys.path 上，于是 `app.*`（顶层）
# 与 `backend.app.*` 是**两个不同的模块对象** —— 单例/settings 各自一份，隔离失效。
pythonpath = ..
testpaths = tests
